import React from "react";
import { getBooksQuery } from "../queries/queries";

const BookListNew = () => {
  return (
    <div>
      <p> books list new</p>
    </div>
  );
};

export default BookListNew;
